# -*- shell-script -*-

mountroot() {
    log_begin_msg "Mounting rescue root"
    mount -t tmpfs none "${rootmnt}"
    rescue_image=$(get_any rescue_image)
    wget -q -O - "${rescue_image}" | tar -C "${rootmnt}" -x -f -
    log_end_msg
}
